// components/AdUnit.js — AdSense wrapper (placeholder)
export default function AdUnit({ slot, format = "auto" }) {
  return (
    <div>
      {/* AdSense ad unit will be configured here */}
    </div>
  );
}
